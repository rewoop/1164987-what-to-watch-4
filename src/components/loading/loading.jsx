import React from "react";
// import PropTypes from "prop-types";

const Loading = (props) => {
  const {} = props;

  return (<h1>Loading...</h1>);
};

// Loading.propTypes = {};

export default Loading;
